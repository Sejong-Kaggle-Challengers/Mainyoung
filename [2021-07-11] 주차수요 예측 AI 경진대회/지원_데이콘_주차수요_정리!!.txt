
1. test셋에서는 서울특별시를 다루고 있지 않음 -> train set에서도 빼 줌

2. 결측치 ==> 0이나 mean값이나 점수차이가 없었어서 0으로 채움 // 신분 결측치는 행 자체를 빼 줌

3. 중복데이터를 제거

4. 오류데이터 있는 단지 제거

5. 지역별 세대당, 면적당 등록차량수 새로운 feature 만들려고 했지만 . . . 실패 . . .

6. 임대건물구분,지역,공급유형을 하나로 묶어서 새로운 피쳐 만듦, 라벨링

7. 지역은 베이스코드 그대로 숫자로 매핑

8. 전용면적 구간 10으로 정해서 매핑 

9. corr 계수 낮은 거 제거

10. 모델은 XGBRegressor + RandomizedSearchCV

11. 검증은 RepeatedKFold


* 모델이랑 검증 : 코드공유에서 참고함



############더 생각해봐야 할 것#############

1. 임대건물구분 : test set에 장기전세, 공공분양, 공공임대(5년)이 없다. 
 ==> train set에서도 빼볼까? .... ? 다른 값으로 대체..? 암튼 이거 조금 고민 해 볼 필요가 있다.

2. 신분 : test set에 B, F 없음 
==> 어떡하지? 

3. age_gender 지역별 매핑해봤지만 좋은 결과가 나오진 않았다. 

############궁금한 것###############

1. train 데이터 자체가 많이 없어서 train_test_split으로 나누기가 좀 애매한데 오차율정도를 확인하고 싶을 때 
   '결과가 좋다/안좋다'를 다들 제출해보고 점수 보고 아시는건지..? ㅠㅡㅠ

2. age_gender 파일을 어떻게 처리했는지 다른 팀은 어떻게 하셨는 지 알고싶다! 